== WADRC Documentation Search

This is a simple textfile crawler designed to pull READMEs and JOURNALs into a searchable database.

Add this to your nightly cron:

rake crawler:crawl_filesystem

Then run the server, and search away.

Erik